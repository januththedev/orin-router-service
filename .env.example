# Orin Router — environment template. NEVER commit real values.
# Vercel → Project → Settings → Environment Variables.

# Neon Postgres (pooled). Then run schema.sql once in the Neon SQL editor.
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require

# Admin bearer for /admin/* (X-Admin-Secret header). Long random value.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ORIN_ROUTER_ADMIN=

# Master key for provider-credential encryption (AES-GCM, min 32 chars).
# Rotating it invalidates stored provider keys — re-save providers after.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ROUTER_MASTER_KEY=

# Optional first-boot seed (applied only when tables are empty):
# {"providers":[{"id":"or1","type":"openrouter","baseUrl":"","apiKey":"sk-or-…","models":[]}],
#  "routes":[{"id":"orin-smart","hops":[{"provider":"or1","model":"x-ai/grok-4-1-fast"}]}]}
ROUTER_SEED_JSON=

# Per-hop cooldown after retryable failures (ms). Default 60000.
ROUTER_COOLDOWN_MS=
