import { RouterError } from "./errors.js";
import type { ChatRequestBody, ImageRequestBody, ProviderAdapter } from "./types.js";
const PNG_1X1 = "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=";
class FakeAdapter implements ProviderAdapter { readonly id = "fake"; async chat(request: ChatRequestBody) { return { text: `fake:${request.messages.at(-1)?.content ?? ""}`, model: "free-text", units: 1 }; } async stream(request: ChatRequestBody, _model: string, onText: (text: string) => void) { const text = `fake:${request.messages.at(-1)?.content ?? ""}`; onText(text); return { text, model: "free-text", units: 1 }; } async image(_request: ImageRequestBody) { return { data: PNG_1X1, model: "free-text", units: 1 }; } }
class OpenRouterAdapter implements ProviderAdapter { readonly id = "openrouter"; private readonly apiKey: string; private readonly fetchImpl: typeof fetch; constructor(apiKey: string, fetchImpl: typeof fetch = fetch) { this.apiKey = apiKey; this.fetchImpl = fetchImpl; } private headers() { return { "content-type": "application/json", authorization: `Bearer ${this.apiKey}`, "http-referer": "https://orinai.org", "x-title": "Orin Router" }; } async chat(request: ChatRequestBody, model: string) { const response = await this.fetchImpl("https://openrouter.ai/api/v1/chat/completions", { method: "POST", headers: this.headers(), body: JSON.stringify({ model, messages: request.messages, temperature: request.temperature, max_tokens: request.max_tokens }) }); if (!response.ok) throw new RouterError("ORIN_PROVIDER_ERROR", `Provider failed (HTTP ${response.status}).`, response.status >= 500 || response.status === 429); const body = await response.json() as { choices?: Array<{ message?: { content?: string } }> }; return { text: body.choices?.[0]?.message?.content ?? "", model, units: 1 }; } async stream(request: ChatRequestBody, model: string, onText: (text: string) => void) { const result = await this.chat(request, model); onText(result.text); return result; } async image(request: ImageRequestBody, model: string) { const response = await this.fetchImpl("https://openrouter.ai/api/v1/images/generations", { method: "POST", headers: this.headers(), body: JSON.stringify({ model, prompt: request.prompt, n: 1, size: request.size, response_format: "b64_json" }) }); if (!response.ok) throw new RouterError("ORIN_PROVIDER_ERROR", `Image provider failed (HTTP ${response.status}).`, response.status >= 500 || response.status === 429); const body = await response.json() as { data?: Array<{ b64_json?: string }> }; return { data: body.data?.[0]?.b64_json ?? "", model, units: 1 }; } }
export function createProviderAdapter(mode: "fake" | "live", apiKey: string | undefined, fetchImpl: typeof fetch = fetch): ProviderAdapter { if (mode === "fake") return new FakeAdapter(); if (!apiKey) throw new Error("ORIN_PROVIDER_API_KEY is required in live mode"); return new OpenRouterAdapter(apiKey, fetchImpl); }
